/* Configuração pública do front-end. Não coloque segredos neste arquivo. */
// Use false para consumir a API Flask; vazio usa a mesma origem da página.
export const USAR_MOCK = true;
export const API_BASE = "";
